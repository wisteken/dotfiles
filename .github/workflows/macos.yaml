name: MacOS

on:
  push: 
    branches: [ master ]

jobs:
  macos:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v1

    - name: Install
      run: |
        chmod +x ./install.sh
        ./install.sh
